//AVANCE ZAMORA
import './Hero.css';

function Hero() {
  return (
    <section id="inicio" className="gamer-hero">
      <div className="hero-content">
        <h2 className="hero-title">Lleva tu experiencia al siguiente nivel</h2>
        <p className="hero-subtitle">Descubre los mejores juegos en RYVEX.</p>
        <a className="hero-btn" href="#catalogo">Ver Catálogo</a>
      </div>
    </section>
  );
}

export default Hero;
